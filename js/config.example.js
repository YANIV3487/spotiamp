// Optional convenience file — copy to config.js and fill in CLIENT_ID if you want
// this browser to default to a specific Spotify Client ID instead of typing it into
// the in-app "Enter your Spotify Client ID" form (config.js is gitignored, so your
// own value never gets committed). Skipping this entirely also works fine — the
// in-app form saves to localStorage, which persists across visits on its own.
const CONFIG = {
  CLIENT_ID: 'YOUR_SPOTIFY_CLIENT_ID_HERE'
};
